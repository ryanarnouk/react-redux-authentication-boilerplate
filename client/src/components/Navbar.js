import React, { Component } from 'react';

// If user is logged in display name and profile picture, if not display "Login" and "Create Account" links

class Navbar extends Component {
  render() { 
    return ( 
      <p>
      </p>
    );
  }
}
 
export default Navbar;